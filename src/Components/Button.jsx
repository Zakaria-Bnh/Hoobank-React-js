
const Button = ({styles}) => (
    <button type='button' className={`w-fit py-4 px-6 bg-blue-gradient font-poppins font-medieum text-[18px] text-primary outline-none ${styles}`}>
      Get Statred
    </button>
  )

export default Button
